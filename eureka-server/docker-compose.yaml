services:
  eureka-server:
    build:
      context: ./eureka
    container_name: eureka-server
    ports:
      - "8761:8761"
    networks:
      - shared-net

networks:
  shared-net:
    external: true
