# Application name
spring.application.name=products

# MVC settings
spring.mvc.throw-exception-if-no-handler-found=true

# Web resources
spring.web.resources.add-mappings=false

# MongoDB connection
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI}
inventory.legacy-product-default-quantity=${LEGACY_PRODUCT_DEFAULT_QUANTITY:1}

# JWT settings
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

# tomcat settings
server.port=8000

# dev tools
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true


# kafka
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.consumer.group-id=products-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.properties.spring.json.trusted.packages=*
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer

# OpenSearch / Elasticsearch-compatible search
search.opensearch.host=${OPENSEARCH_HOST:opensearch}
search.opensearch.port=${OPENSEARCH_PORT:9200}
search.opensearch.scheme=${OPENSEARCH_SCHEME:http}
search.products.index=${PRODUCT_SEARCH_INDEX:products-search}


# eureka
eureka.client.service-url.defaultZone=${DISCOVERY}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.hostname=products-service
eureka.instance.non-secure-port-enabled=false
eureka.instance.secure-port-enabled=true
eureka.instance.secure-port=${server.port}

server.ssl.enabled=true
server.ssl.key-store-password=${KEY_STORE_PWD}
server.ssl.key-store-type=${KEY_STORE_TYPE}

# For mTLS — require client cert from gateway
server.ssl.client-auth=need
server.ssl.key-store=${CERTIFICATE_PATH:file:/app/resources/certs/products-service.p12}
server.ssl.trust-store=${TRUSTSTORE_PATH:file:/app/resources/certs/truststore.p12}
server.ssl.trust-store-password=${TRUST_STORE_PWD}

management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.prometheus.enabled=true
management.metrics.tags.application=products-service
management.observations.key-values.service=products-service
management.metrics.distribution.percentiles-histogram.http.server.requests=true
management.metrics.distribution.percentiles-histogram.marketplace.inventory.stock.reserve=true

spring.redis.host=${REDIS_HOST}
spring.redis.port=${REDIS_PORT}