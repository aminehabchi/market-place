spring.application.name=users
spring.devtools.restart.enabled=true
spring.devtools.restart.poll-interval=2s
spring.devtools.restart.quiet-period=1s
# Kafka bootstrap servers (adjust host/port if necessary)
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

# Producer configuration
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.producer.properties.spring.json.add.type.headers=false
jwt.private-key=classpath:keys/private.pem

# tomcat settings
server.port=8080

# eureka
eureka.client.service-url.defaultZone=${DISCOVERY}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
server.ssl.enabled=true
server.ssl.key-store-password=123123
server.ssl.key-store-type=PKCS12

# For mTLS — require client cert from gateway
server.ssl.client-auth=need
server.ssl.key-store=file:/app/resources/certs/users-service.p12
server.ssl.trust-store=file:/app/resources/certs/truststore.p12
server.ssl.trust-store-password=123123